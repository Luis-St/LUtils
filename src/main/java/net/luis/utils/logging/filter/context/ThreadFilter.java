package net.luis.utils.logging.filter.context;

import net.luis.utils.logging.filter.LogFilter;

/**
 *
 * @author Luis-St
 *
 */

// Idea: Create a filter that filters log messages based on the thread (record ThreadInfo)
public abstract class ThreadFilter implements LogFilter {}
